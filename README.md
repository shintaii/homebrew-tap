# Homebrew Tap for EnvManager CLI

## Install

```bash
brew install shintaii/tap/envmanager
```

## Upgrade

```bash
brew upgrade envmanager
```

## About

This tap is automatically updated when a new version of the [EnvManager CLI](https://www.npmjs.com/package/@envmanager-cli/cli) is published to npm.
